"""LLM curation helpers placeholders."""

from __future__ import annotations


async def normalize_supply_chain_payload(raw_payload: dict) -> dict:
    """Normalize raw retrieval data into stable contract shape."""
    return raw_payload
